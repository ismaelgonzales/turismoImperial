import { environment } from '../../../environments/environment';

export const ApiUrlConstants = {
    acceso: `${environment.endPoint}Acceso/Login`,
};

export const SessionConstants = {
    token: 'token',
};
